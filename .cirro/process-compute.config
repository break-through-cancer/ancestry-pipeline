process {
    withName: phase_with_eagle {
        cpus = { 2 * task.attempt }
        memory = { 32.GB * task.attempt }
    }
    maxRetries = 2
    executor = 'awsbatch'
    errorStrategy = 'retry'
    container = 'ghcr.io/jchen1095/ancestry-pipeline:latest'
}
docker {
    enabled = true
}